services:
  backend:
    build: .
    volumes:
      - .:/app
      - ${HOME:-C:/Users/mina_}/.config/earthengine:/root/.config/earthengine
      - ./logs:/app/logs
      - ./outputs:/app/outputs
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: always
  # Future: Add Database and Frontend services here
  # db:
  #   image: postgres:15
  #   ...
